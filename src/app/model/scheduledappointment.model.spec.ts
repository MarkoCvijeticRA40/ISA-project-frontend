import { Scheduledappointment } from './scheduledappointment.model';

describe('Scheduledappointment', () => {
  it('should create an instance', () => {
    expect(new Scheduledappointment()).toBeTruthy();
  });
});
