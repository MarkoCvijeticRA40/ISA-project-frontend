import { ScheduldedAppointmentCalendar } from './schedulded-appointment-calendar.model';

describe('ScheduldedAppointmentCalendar', () => {
  it('should create an instance', () => {
    expect(new ScheduldedAppointmentCalendar()).toBeTruthy();
  });
});
